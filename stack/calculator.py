def calculate(s: str) -> int:
    """
    Вычисляет результат арифметического выражения, содержащего +, -, ().

    🔹 **Метод**: Используем стек для обработки скобок
    🔹 **Сложность**: O(n) — один проход по строке
    🔹 **Память**: O(n) — стек может хранить все вложенные скобки

    ✅ **Пример работы**:
       ```
       Вход: "1 + 1" → Выход: 2
       Вход: " 2-1 + 2 " → Выход: 3
       Вход: "(1+(4+5+2)-3)+(6+8)" → Выход: 23
       Вход: "10 - (2 + 3)" → Выход: 5
       Вход: "((1+2)+3)" → Выход: 6
       ```
    """
    stack = []
    num = 0
    result = 0  # Текущая сумма
    sign = 1  # 1 = "+", -1 = "-"

    for char in s:
        if char.isdigit():  # Собираем число
            num = num * 10 + int(char)
        elif char in "+-":  # Оператор
            result += sign * num  # Добавляем предыдущее число
            num = 0
            sign = 1 if char == "+" else -1  # Обновляем знак
        elif char == "(":  # Открывающая скобка
            stack.append(result)  # Сохраняем текущую сумму
            stack.append(sign)  # Сохраняем текущий знак
            result = 0
            sign = 1
        elif char == ")":  # Закрывающая скобка
            result += sign * num  # Обновляем результат скобки
            result *= stack.pop()  # Применяем знак перед скобками
            result += stack.pop()  # Добавляем сумму до скобок
            num = 0

    return result + sign * num  # Обрабатываем последнее число

# 🔹 Тестируем
print(calculate("1 + 1"))  # ✅ 2
print(calculate(" 2-1 + 2 "))  # ✅ 3
print(calculate("(1+(4+5+2)-3)+(6+8)"))  # ✅ 23
print(calculate("10 - (2 + 3)"))  # ✅ 5
print(calculate("100"))  # ✅ 100
print(calculate("(10)"))  # ✅ 10
print(calculate("((1+2)+3)"))  # ✅ 6
