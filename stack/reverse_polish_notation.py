def eval_rpn(tokens: list[str]) -> int:
    """
    Вычисляет значение выражения в обратной польской нотации (RPN).

    🔹 **Аргументы**:
        - `tokens`: список строк, содержащий операнды и операторы.

    🔹 **Метод**:
        - Использует стек (`stack`), куда кладёт числа.
        - При встрече оператора `+`, `-`, `*`, `/` берёт два числа из стека.
        - Выполняет операцию и кладёт результат обратно в стек.

    🔹 **Сложность**:
        - `O(n)`, где `n` — количество токенов.

    ✅ **Пример работы**:
       ```
       eval_rpn(["2", "1", "+", "3", "*"])  # 9
       eval_rpn(["4", "13", "5", "/", "+"])  # 6
       ```
    """
    stack = []

    for token in tokens:
        if token in {"+", "-", "*", "/"}:  # Если оператор
            b, a = stack.pop(), stack.pop()  # Берём два верхних числа
            if token == "+":
                res = a + b
            elif token == "-":
                res = a - b
            elif token == "*":
                res = a * b
            else:
                res = int(a / b)  # Округление к 0
            stack.append(res)  # Кладём результат обратно
        else:
            stack.append(int(token))  # Операнд → в стек

    return stack[0]  # Итоговый результат


# 🔹 Тесты
print(eval_rpn(["2", "1", "+", "3", "*"]))  # ✅ 9   → (2 + 1) * 3
print(eval_rpn(["4", "13", "5", "/", "+"]))  # ✅ 6   → 4 + (13 / 5)
print(eval_rpn(["10", "6", "9", "3", "/", "-", "*"]))  # ✅ 30 → 10 * (6 - (9 / 3))
print(eval_rpn(["3", "-4", "+"]))  # ✅ -1 → 3 + (-4)
print(eval_rpn(["5", "1", "2", "+", "4", "*", "+", "3", "-"]))  # ✅ 14
