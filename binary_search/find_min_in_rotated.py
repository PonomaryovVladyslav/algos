# Задача: Найти минимальный элемент в отсортированном, но повернутом массиве.
# Дано: массив `nums`, отсортированный по возрастанию, но с возможным сдвигом.
# Нужно: вернуть минимальный элемент массива.

def find_min(nums: list[int]) -> int:
    """
    Функция находит минимальный элемент в отсортированном, но повернутом массиве.

    :param nums: list[int] - отсортированный и повернутый массив.
    :return: int - минимальный элемент массива.
    """

    left, right = 0, len(nums) - 1  # Границы бинарного поиска

    while left < right:
        mid = (left + right) // 2  # Средний индекс

        if nums[mid] > nums[right]:  # Минимум находится справа
            left = mid + 1
        else:  # Минимум слева или mid
            right = mid

    return nums[left]  # `left == right`, указываем на минимум


# 🔹 Тестируем
print(find_min([3, 4, 5, 1, 2]))  # Ожидаемый результат: 1
print(find_min([4, 5, 6, 7, 0, 1, 2]))  # Ожидаемый результат: 0
print(find_min([11, 13, 15, 17]))  # Ожидаемый результат: 11
print(find_min([2, 3, 4, 5, 6, 7, 0, 1]))  # Ожидаемый результат: 0
print(find_min([1]))  # Ожидаемый результат: 1

"""
Анализ сложности:
- Временная сложность: O(log n), так как используется бинарный поиск.
- Пространственная сложность: O(1), так как не используется дополнительная память.
"""
