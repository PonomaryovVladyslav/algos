# Задача: Найти индекс пикового элемента массива.
# Дано: массив `nums`, в котором `nums[i]` ≠ `nums[i+1]` для всех `i`.
# Нужно: вернуть индекс `i`, где `nums[i]` — локальный пик (nums[i] > nums[i-1] и nums[i] > nums[i+1]).

def find_peak_element(nums: list[int]) -> int:
    """
    Функция находит индекс пикового элемента в `nums`.

    :param nums: list[int] - список чисел.
    :return: int - индекс пикового элемента.
    """

    left, right = 0, len(nums) - 1  # Границы бинарного поиска

    while left < right:
        mid = (left + right) // 2  # Средний индекс

        if nums[mid] < nums[mid + 1]:  # Если правый сосед больше — пик справа
            left = mid + 1
        else:  # Если `nums[mid] > nums[mid + 1]`, то пик слева или в `mid`
            right = mid

    return left  # `left == right`, значит нашли пик


# 🔹 Тестируем
print(find_peak_element([1, 2, 3, 1]))  # Ожидаемый результат: 2
print(find_peak_element([1, 2, 1, 3, 5, 6, 4]))  # Ожидаемый результат: 1 или 5
print(find_peak_element([10, 20, 15, 2, 23, 90, 67]))  # Ожидаемый результат: 1, 5 или 6
print(find_peak_element([1]))  # Ожидаемый результат: 0 (только один элемент)

"""
Анализ сложности:
- Временная сложность: O(log n), так как используется бинарный поиск.
- Пространственная сложность: O(1), так как не используется дополнительная память.
"""
