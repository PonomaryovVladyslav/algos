# Задача: Найти индекс `target` в отсортированном массиве `nums` или позицию, куда его можно вставить.
# Дано: массив `nums`, отсортированный по возрастанию, и число `target`.
# Нужно: вернуть индекс `target`, если он найден, иначе вернуть индекс, куда его можно вставить.

def search_insert(nums: list[int], target: int) -> int:
    """
    Функция выполняет бинарный поиск, чтобы найти `target` в `nums` или определить его позицию вставки.

    :param nums: list[int] - отсортированный список чисел.
    :param target: int - искомое число.
    :return: int - индекс `target` или позиция его вставки.
    """

    left, right = 0, len(nums) - 1  # Границы бинарного поиска

    while left <= right:
        mid = (left + right) // 2  # Средний индекс

        if nums[mid] == target:
            return mid  # Найдено точное совпадение
        elif nums[mid] < target:
            left = mid + 1  # Ищем справа
        else:
            right = mid - 1  # Ищем слева

    return left  # `left` указывает на место вставки


# 🔹 Тестируем
print(search_insert([1, 3, 5, 6], 5))  # Ожидаемый результат: 2
print(search_insert([1, 3, 5, 6], 2))  # Ожидаемый результат: 1
print(search_insert([1, 3, 5, 6], 7))  # Ожидаемый результат: 4
print(search_insert([1, 3, 5, 6], 0))  # Ожидаемый результат: 0
print(search_insert([1], 0))  # Ожидаемый результат: 0

"""
Анализ сложности:
- Временная сложность: O(log n), так как используется бинарный поиск.
- Пространственная сложность: O(1), так как не используется дополнительная память.
"""
