def happy_numbers(n: int) -> bool:
    """
    Определяет, является ли число `n` счастливым.

    🔹 **Алгоритм (O(log n))**:
    1️⃣ Создаём `set()` для хранения уже встреченных чисел.
    2️⃣ Пока `n` не равно `1` и не зациклилось:
       - Разбиваем `n` на цифры, возводим их в квадрат и суммируем.
       - Если результат уже был в `used_set`, значит, цикл → `False`.
       - Добавляем новое значение в `used_set` и продолжаем.
    3️⃣ Если дошли до `1`, число счастливое → `True`.

    🔹 **Временная сложность**:
       - `O(log n)`, так как количество итераций пропорционально количеству цифр в `n`.

    🔹 **Пространственная сложность**:
       - `O(log n)`, так как храним `set()` размером не более количества цифр в числе.

    :param n: int - Число для проверки.
    :return: bool - `True`, если число счастливое, иначе `False`.
    """
    used_set = set()

    while n != 1 and n not in used_set:
        used_set.add(n)  # Запоминаем числа, чтобы обнаружить цикл
        n = sum(int(digit) ** 2 for digit in str(n))  # Считаем сумму квадратов цифр

    return n == 1  # Если дошли до 1, число счастливое


# 🔹 Тесты
print(happy_numbers(19))  # ✅ True (19 → 82 → 68 → 100 → 1)
print(happy_numbers(2))   # ✅ False (2 → 4 → 16 → 37 → 58 → 89 → 145 → 42 → 20 → 4 (цикл))
print(happy_numbers(7))   # ✅ True (7 → 49 → 97 → 130 → 10 → 1)
print(happy_numbers(1))   # ✅ True (1 — счастливое число)
