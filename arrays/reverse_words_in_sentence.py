# Задача: Развернуть порядок слов в строке.
# Дано: строка `s`, содержащая слова, разделенные пробелами.
# Нужно: вернуть строку, в которой порядок слов изменен на обратный, а лишние пробелы удалены.

def reverse_words(s: str) -> str:
    """
    Функция разворачивает порядок слов в строке.

    :param s: str - входная строка.
    :return: str - строка с перевернутым порядком слов.
    """

    return " ".join(s.strip().split()[::-1])  # Убираем лишние пробелы, разбиваем строку на слова, переворачиваем их и объединяем обратно


# 🔹 Тестируем
s1 = "the sky is blue"
s2 = "  hello world  "
s3 = "a good   example"

print(reverse_words(s1))  # Ожидаемый результат: "blue is sky the"
print(reverse_words(s2))  # Ожидаемый результат: "world hello"
print(reverse_words(s3))  # Ожидаемый результат: "example good a"

"""
Анализ сложности:
- Временная сложность: O(n), так как split() и join() работают за O(n).
- Пространственная сложность: O(n), так как создается новый список слов.
"""
