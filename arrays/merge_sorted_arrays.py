# Задача: Слить два отсортированных массива nums1 и nums2 в один, сохраняя порядок.
# Дано:
# - nums1 имеет длину m + n, первые m элементов содержат числа, остальные n элементов — нули (место для nums2).
# - nums2 содержит n элементов, и все они отсортированы.
# Нужно объединить nums2 в nums1 в отсортированном порядке.

def merge_sorted_arrays(nums1: list[int], nums2: list[int], m: int, n: int) -> None:
    """
    Функция сливает два отсортированных массива nums1 и nums2 в nums1.

    :param nums1: list[int] - первый массив длины (m + n), содержащий m элементов и n свободных мест (нулей).
    :param nums2: list[int] - второй массив длины n, содержащий n отсортированных элементов.
    :param m: int - количество фактических элементов в nums1.
    :param n: int - количество элементов в nums2.
    :return: None - массив nums1 изменяется на месте.
    """

    i: int = m - 1  # Индекс последнего элемента в nums1 (исходные данные)
    j: int = n - 1  # Индекс последнего элемента в nums2
    k: int = m + n - 1  # Индекс последней позиции в nums1 (куда вставлять элементы)

    # Двигаемся с конца, чтобы не затирать элементы в nums1
    while j >= 0:
        if i >= 0 and nums1[i] > nums2[j]:
            nums1[k] = nums1[i]  # Если элемент в nums1 больше, перемещаем его
            i -= 1
        else:
            nums1[k] = nums2[j]  # Иначе берем элемент из nums2
            j -= 1
        k -= 1  # Переход к следующему месту

# 🔹 Тестируем
nums1 = [1, 3, 5, 0, 0, 0]
nums2 = [2, 4, 6]
m, n = 3, 3

merge_sorted_arrays(nums1, nums2, m, n)
print(nums1)  # Ожидаемый результат: [1, 2, 3, 4, 5, 6]

"""
Анализ сложности:
- Временная сложность: O(m + n), так как мы проходим оба массива один раз.
- Пространственная сложность: O(1), так как слияние выполняется на месте без использования дополнительной памяти.
"""
