# Задача: Найти максимальную прибыль от покупки и продажи акции с неограниченным числом сделок.
# Дано: список цен `prices`, где `prices[i]` — цена акции в день `i`.
# Нужно: найти максимальную возможную прибыль, совершая несколько покупок и продаж.

def max_profit(prices: list[int]) -> int:
    """
    Функция вычисляет максимальную прибыль от торговли акциями с неограниченным числом сделок.

    :param prices: list[int] - список цен акции по дням.
    :return: int - максимальная возможная прибыль.
    """

    profit: int = 0

    # Проходим по массиву, если цена растет — фиксируем прибыль
    for i in range(1, len(prices)):  # Начинаем с 1, так как сравниваем с предыдущим днём
        if prices[i] > prices[i - 1]:  # Если цена выросла — продаем
            profit += prices[i] - prices[i - 1]  # Фиксируем прибыль

    return profit


# 🔹 Тестируем
print(max_profit([7, 1, 5, 3, 6, 4]))  # Ожидаемый результат: 7 (покупка на 1, продажа на 5 + покупка на 3, продажа на 6)
print(max_profit([1, 2, 3, 4, 5]))    # Ожидаемый результат: 4 (постепенная покупка на 1, продажа на 5)
print(max_profit([7, 6, 4, 3, 1]))    # Ожидаемый результат: 0 (цена падает, нет смысла торговать)
print(max_profit([1, 7, 2, 3, 6, 7, 6, 7]))  # Ожидаемый результат: 10

"""
Анализ сложности:
- Временная сложность: O(n), так как мы проходим массив один раз.
- Пространственная сложность: O(1), так как используем только одну переменную `profit`.
"""
