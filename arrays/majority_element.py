# Задача: Найти элемент, который встречается более чем ⌊n / 2⌋ раз (мажоритарный элемент).
# Дано: список чисел `nums`.
# Нужно вернуть число, которое появляется в списке более чем n / 2 раз.
# Гарантируется, что такой элемент всегда существует.

def find_majority(nums: list[int]) -> int:
    """
    Функция находит мажоритарный элемент (элемент, встречающийся более чем n/2 раз).

    :param nums: list[int] - список чисел.
    :return: int - мажоритарный элемент.
    """

    count: int = 0
    candidate: int | None = None  # Переменная для хранения потенциального кандидата

    # Алгоритм Бойера-Мура для поиска мажоритарного элемента
    for num in nums:
        if count == 0:  # Если счетчик сброшен, назначаем нового кандидата
            candidate = num
        count += 1 if num == candidate else -1  # Увеличиваем/уменьшаем счетчик

    return candidate  # Гарантировано, что элемент существует


# 🔹 Тестируем
nums = [2, 2, 1, 1, 1, 2, 2, 3, 5, 6, 2, 2, 2, 2]
print(find_majority(nums))  # Ожидаемый результат: 2

"""
Анализ сложности:
- Временная сложность: O(n), так как мы проходим массив один раз.
- Пространственная сложность: O(1), так как используем только две переменные (`candidate` и `count`).
"""
