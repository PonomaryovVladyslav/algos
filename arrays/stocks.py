# Задача: Найти максимальную прибыль от покупки и продажи акции.
# Дано: список цен `prices`, где `prices[i]` — цена акции в день `i`.
# Нужно: найти максимальную разницу между ценой продажи и покупки (покупка должна быть раньше продажи).

def max_profit(prices: list[int]) -> int:
    """
    Функция вычисляет максимальную прибыль от одной покупки и одной продажи акции.

    :param prices: list[int] - список цен акции по дням.
    :return: int - максимальная возможная прибыль.
    """

    if not prices:
        return 0  # Если список пуст, прибыли нет

    min_price: int = float('inf')  # Минимальная цена для покупки
    max_profit: int = 0  # Максимальная возможная прибыль

    # Проходим по списку и обновляем min_price и max_profit
    for price in prices:
        min_price = min(min_price, price)  # Обновляем минимальную цену покупки
        max_profit = max(max_profit, price - min_price)  # Проверяем текущую прибыль

    return max_profit


# 🔹 Тестируем
print(max_profit([1, 100, 0, 50]))  # Ожидаемый результат: 50 (покупка по 0, продажа по 50)
print(max_profit([7, 1, 5, 3, 6, 4]))  # Ожидаемый результат: 5 (покупка по 1, продажа по 6)
print(max_profit([7, 6, 4, 3, 1]))  # Ожидаемый результат: 0 (нет возможности получить прибыль)

"""
Анализ сложности:
- Временная сложность: O(n), так как мы проходим массив один раз.
- Пространственная сложность: O(1), так как используем только две переменные.
"""
