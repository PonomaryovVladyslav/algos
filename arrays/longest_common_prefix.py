# Задача: Найти самый длинный общий префикс среди списка строк.
# Дано: список строк `strs`.
# Нужно вернуть самую длинную начальную последовательность символов, общую для всех строк.

def longest_common_prefix(strs: list[str]) -> str:
    """
    Функция находит самый длинный общий префикс среди списка строк.

    :param strs: list[str] - список строк.
    :return: str - общий префикс, либо пустая строка, если префикса нет.
    """

    if not strs:
        return ""  # Если список пуст, возвращаем пустую строку

    prefix: str = strs[0]  # Берем первую строку как начальный префикс

    # Сравниваем префикс с каждой следующей строкой
    for s in strs[1:]:
        while prefix and prefix != s[:len(prefix)]:  # Пока префикс не совпадает
            prefix = prefix[:-1]  # Обрезаем последний символ

    return prefix  # Возвращаем найденный общий префикс


# 🔹 Тестируем
strs = ["flower", "flow", "flight"]
print(longest_common_prefix(strs))  # Ожидаемый результат: "fl"

strs2 = ["dog", "racecar", "car"]
print(longest_common_prefix(strs2))  # Ожидаемый результат: "" (нет общего префикса)

strs3 = ["interview", "internet", "internal"]
print(longest_common_prefix(strs3))  # Ожидаемый результат: "inte"

"""
Анализ сложности:
- Временная сложность: O(n * m), где `n` — количество строк, `m` — длина самой длинной строки.
- Пространственная сложность: O(1), так как не используем дополнительную память, кроме переменной `prefix`.
"""
