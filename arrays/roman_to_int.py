# Задача: Конвертация римского числа в целое.
# Дано: строка `s`, представляющая число в римской системе.
# Нужно: вернуть целочисленное представление римского числа.

def roman_to_int(s: str) -> int:
    """
    Функция конвертирует римское число в целое.

    :param s: str - строка с римским числом.
    :return: int - целочисленное значение римского числа.
    """

    roman: dict[str, int] = {
        'I': 1, 'V': 5, 'X': 10, 'L': 50, 'C': 100, 'D': 500, 'M': 1000
    }

    total: int = 0

    # Проходим по строке, обрабатывая символы римского числа
    for i in range(len(s)):
        # Если текущий символ меньше следующего, значит, это вычитательная пара (например, IV = 4)
        if i < len(s) - 1 and roman[s[i]] < roman[s[i + 1]]:
            total -= roman[s[i]]  # Вычитаем значение
        else:
            total += roman[s[i]]  # В остальных случаях прибавляем

    return total


# 🔹 Тестируем
print(roman_to_int("III"))    # Ожидаемый результат: 3
print(roman_to_int("IV"))     # Ожидаемый результат: 4
print(roman_to_int("IX"))     # Ожидаемый результат: 9
print(roman_to_int("LVIII"))  # Ожидаемый результат: 58
print(roman_to_int("MCMXCIV"))# Ожидаемый результат: 1994

"""
Анализ сложности:
- Временная сложность: O(n), так как мы проходим по строке один раз.
- Пространственная сложность: O(1), так как используем фиксированную дополнительную память.
"""
