# Задача: Найти максимальную сумму подмассива фиксированной длины `k`.
# Дано: список чисел `nums` и целое число `k`.
# Нужно найти максимальную сумму `k` последовательных элементов в `nums`.

def max_subarray_sum(nums: list[int], k: int) -> int:
    """
    Функция находит максимальную сумму подмассива длиной `k`.

    :param nums: list[int] - список чисел.
    :param k: int - длина подмассива.
    :return: int - максимальная сумма подмассива длиной `k`.
    """

    if len(nums) < k:
        return 0  # Если массив короче `k`, вернуть 0

    s: int = sum(nums[:k])  # Начальная сумма первых `k` элементов
    max_sum: int = s  # Максимальная сумма, начинаем с первой `k`-группы

    # Используем скользящее окно: добавляем новый элемент, убираем старый
    for i in range(k, len(nums)):
        s += nums[i] - nums[i - k]  # Обновляем сумму окна
        max_sum = max(max_sum, s)  # Обновляем максимум, если нужно

    return max_sum


# 🔹 Тестируем
nums1 = [2, 3, 5, 1, 6, 2, 4]
k1 = 3
print(max_subarray_sum(nums1, k1))  # Ожидаемый результат: 12 (подмассив [5, 1, 6])

nums2 = [1, 9, 3, 8, 2, 6]
k2 = 2
print(max_subarray_sum(nums2, k2))  # Ожидаемый результат: 11 (подмассив [9, 3])

"""
Анализ сложности:
- Временная сложность: O(n), так как мы проходим массив один раз.
- Пространственная сложность: O(1), так как используем только переменные для хранения текущей суммы.
"""
