# 🔹 Задача: Преобразовать отсортированный список `nums` в **сбалансированное бинарное дерево поиска (BST)**.
# 🔹 Дано: `nums` — список, отсортированный по возрастанию.
# 🔹 Нужно: вернуть **корень сбалансированного BST**.

class TreeNode:
    """
    Класс узла бинарного дерева.
    """
    def __init__(self, val: int = 0, left: "TreeNode" | None = None, right: "TreeNode" | None = None):
        self.val = val
        self.left = left
        self.right = right


def sorted_list_to_bst(nums: list[int]) -> TreeNode | None:
    """
    Функция строит сбалансированное бинарное дерево поиска (BST) из отсортированного списка.

    🔹 Алгоритм:
       1️⃣ Выбираем **средний элемент** `mid` как корень BST.
       2️⃣ Рекурсивно строим **левое поддерево** из первой половины `nums[:mid]`.
       3️⃣ Рекурсивно строим **правое поддерево** из второй половины `nums[mid+1:]`.
       4️⃣ Повторяем, пока список не станет пустым.

    🔹 Временная сложность: **O(n)**, так как каждый элемент используется **ровно один раз**.
    🔹 Пространственная сложность: **O(log n)**, так как глубина рекурсии **O(log n)**.

    :param nums: list[int] - отсортированный список значений.
    :return: TreeNode | None - корень сбалансированного BST.
    """

    def generate_node(left: int, right: int) -> TreeNode | None:
        """
        Рекурсивная функция для создания BST.

        :param left: int - индекс начала подмассива.
        :param right: int - индекс конца подмассива.
        :return: TreeNode | None - корень построенного поддерева.
        """
        if left > right:  # Базовый случай (пустой подмассив)
            return None

        mid: int = (left + right) // 2  # Выбираем середину как корень
        node: TreeNode = TreeNode(nums[mid])

        # Рекурсивно создаём левое и правое поддерево
        node.left = generate_node(left, mid - 1)
        node.right = generate_node(mid + 1, right)

        return node

    return generate_node(0, len(nums) - 1)


# 🔹 Тестируем
l1 = [-6, -3, -1, 0, 3, 4, 10, 25]

bst_root = sorted_list_to_bst(l1)
print(bst_root)  # Вывод объекта TreeNode (корня BST)
print("Готово")

"""
🔹 Анализ сложности:

✅ Временная сложность: **O(n)**
   - Мы создаём ровно **n узлов**.
   - Каждую операцию вставки выполняем за **O(1)**.
   - Итоговая сложность: **O(n)**.

✅ Пространственная сложность: **O(log n)**
   - Максимальная глубина рекурсии **O(log n)** (половинное деление массива).
   - Используем **O(1)** доп. памяти, так как не копируем массивы.

📌 **Как работает алгоритм?**
1️⃣ **Выбираем середину списка** как корень.  
2️⃣ **Рекурсивно строим левое поддерево** из **первой половины** списка.  
3️⃣ **Рекурсивно строим правое поддерево** из **второй половины** списка.  
4️⃣ **Повторяем, пока не построим всё дерево**.  

💡 **Почему этот метод работает?**
- Он **гарантирует сбалансированность** BST, так как середина списка всегда становится корнем.
- **Бинарный поиск** (`mid = (left + right) // 2`) даёт **оптимальную структуру дерева**.

"""
