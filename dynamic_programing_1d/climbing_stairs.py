def climb_stairs(n: int) -> int:
    """
    Функция для нахождения количества способов подняться на `n` ступенек.

    🔹 Алгоритм:
    1️⃣ Если `n = 1`, то существует **1 способ** (просто шагнуть на 1 ступеньку).
    2️⃣ Если `n = 2`, то существует **2 способа** (два шага по 1 или один шаг на 2).
    3️⃣ Для `n >= 3` количество способов определяется по формуле **Fibonacci**:
        - `ways(n) = ways(n-1) + ways(n-2)`, так как можно:
          - Дойти до `n-1`, а затем сделать шаг `+1`.
          - Дойти до `n-2`, а затем сделать шаг `+2`.
    4️⃣ Решаем задачу **итеративно**, храняя только **два последних** значения (`n-1` и `n-2`).
    5️⃣ Перебираем числа от 3 до `n`, обновляя значения по формуле **Fibonacci**.

    🔹 Временная сложность: **O(n)** — проходим по числам от 1 до `n` один раз.
    🔹 Пространственная сложность: **O(1)** — используем только две переменные.

    :param n: int - количество ступенек.
    :return: int - количество способов подняться на `n` ступенек.
    """

    if n <= 2:
        return n  # Для n = 1 → 1 способ, для n = 2 → 2 способа

    a, b = 1, 2  # Стартовые значения: 1 ступенька → 1 способ, 2 ступеньки → 2 способа
    for _ in range(3, n + 1):  # Начиная с 3 ступенек
        a, b = b, a + b  # Обновляем два последних значения (сумма способов для n-1 и n-2)

    return b  # Возвращаем количество способов для n


# 🔹 Тестируем
print(climb_stairs(2))  # 2
print(climb_stairs(3))  # 3
print(climb_stairs(4))  # 5
print(climb_stairs(5))  # 8
print(climb_stairs(10))  # 89
