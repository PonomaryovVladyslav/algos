def min_coin_amount(coins: list[int], value: int) -> tuple[int, list[int]]:
    """
    Функция находит минимальное количество монет для суммы `value` и какие монеты использовались.

    🔹 Алгоритм:
    1️⃣ Используем динамическое программирование.
    2️⃣ Создаём массив `dp`, где `dp[i]` — минимальное количество монет для суммы `i`.
    3️⃣ Инициализируем `dp[0] = 0` (0 монет для суммы 0), остальные `dp[i] = ∞`.
    4️⃣ Для каждой суммы `i` проверяем все доступные монеты:
        - Если `i >= coin`, обновляем `dp[i] = min(dp[i], dp[i - coin] + 1)`.
    5️⃣ Восстанавливаем список использованных монет:
        - Начинаем с `i = value`, двигаемся назад, проверяя, какие монеты привели к `dp[i]`.

    🔹 Временная сложность: **O(n * m)**, где `n = value`, `m = len(coins)`.
    🔹 Пространственная сложность: **O(n)`, так как используем массив `dp` размера `value + 1`.

    :param coins: list[int] - список доступных номиналов монет.
    :param value: int - целевая сумма.
    :return: tuple[int, list[int]] - минимальное количество монет и список использованных монет.
    """

    # 1. Инициализируем dp-массив бесконечностями
    dp = [float('inf')] * (value + 1)
    dp[0] = 0  # Для суммы 0 монет не требуется

    # 2. Заполняем dp массив
    for i in range(1, value + 1):
        for coin in coins:
            if i >= coin:
                dp[i] = min(dp[i], dp[i - coin] + 1)

    # 3. Если сумму невозможно собрать, возвращаем -1
    if dp[value] == float('inf'):
        return -1, []

    # 4. Восстанавливаем список использованных монет
    used_coins = []
    i = value
    while i > 0:
        for coin in coins:
            if i >= coin and dp[i] == dp[i - coin] + 1:
                used_coins.append(coin)
                i -= coin
                break  # Найденную монету сразу добавляем

    return dp[value], used_coins


# 🔹 Тестируем
coins = [1, 2, 5, 10]
value = 123

print(min_coin_amount(coins, value))  # (13, [10, 10, 10, 10, 10, 10, 10, 10, 10, 10, 2, 1])
