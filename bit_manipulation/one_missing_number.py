# Задача: Найти отсутствующее число в массиве `nums`, содержащем `n` уникальных чисел от `0` до `n`.
# Дано: список `nums` длины `n`, содержащий все числа от `0` до `n`, кроме одного.
# Нужно: вернуть это отсутствующее число.

def find_missing_number_xor(nums: list[int]) -> int:
    """
    Функция находит отсутствующее число в массиве `nums` (используя XOR).

    🔹 Временная сложность: O(n), так как мы проходим по массиву дважды.
    🔹 Пространственная сложность: O(1), так как используем только две переменные.

    :param nums: list[int] - массив из `n` уникальных чисел от `0` до `n`, в котором отсутствует одно число.
    :return: int - отсутствующее число.
    """

    n: int = len(nums)
    xor_all: int = 0
    xor_nums: int = 0

    # XOR всех чисел от 0 до n
    for i in range(n + 1):
        xor_all ^= i

    # XOR всех чисел в массиве
    for num in nums:
        xor_nums ^= num

    return xor_all ^ xor_nums  # Найдём отсутствующее число


# 🔹 Тест
nums = [9, 6, 4, 2, 3, 5, 7, 0, 1]
print(find_missing_number_xor(nums))  # ✅ Ожидаемый результат: 8


"""
🔹 Анализ сложности:

✅ Временная сложность: **O(n)**
   - Два прохода по массиву: один для `xor_all`, второй для `xor_nums`.
   - Итоговая сложность: **O(n) + O(n) = O(n)**.

✅ Пространственная сложность: **O(1)**
   - Используем **только две переменные** (`xor_all`, `xor_nums`).

📌 **Как работает XOR?**
1️⃣ `A ^ A = 0` (XOR одинаковых чисел даёт `0`).  
2️⃣ `A ^ 0 = A` (XOR с `0` оставляет число неизменным).  
3️⃣ **Порядок операций не важен** (XOR коммутативен и ассоциативен).

💡 **Почему этот метод работает?**
- Мы берём **XOR всех чисел от `0` до `n`** и **XOR всех чисел в `nums`**.
- Все **числа, которые есть в `nums`**, исключаются (`x ^ x = 0`).
- Остаток после XOR — **отсутствующее число**.

"""
