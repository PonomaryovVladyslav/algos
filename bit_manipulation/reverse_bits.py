# Задача: Развернуть двоичное представление 32-битного числа.
# Дано: целое число `n`, представленное 32-битным беззнаковым целым числом.
# Нужно: вернуть его двоичное представление в **обратном порядке**.

def reverse_bits(n: int) -> int:
    """
    Функция выполняет реверс битов 32-битного числа.

    🔹 Временная сложность: O(1), так как выполняем 32 итерации (константное время).
    🔹 Пространственная сложность: O(1), так как используем только две переменные.

    :param n: int - беззнаковое 32-битное целое число.
    :return: int - число с развернутыми битами.
    """

    result: int = 0
    for i in range(32):
        result = (result << 1) | (n & 1)  # Добавляем последний бит `n` в `result`
        n >>= 1  # Сдвигаем `n` вправо
    return result


# 🔹 Тестируем
print(reverse_bits(0b00000010100101000001111010011100))  # ✅ Ожидаемый результат: 964176192
print(reverse_bits(0b11111111111111111111111111111101))  # ✅ Ожидаемый результат: 3221225471


"""
🔹 Анализ сложности:

✅ Временная сложность: **O(1)**
   - Фиксированное количество итераций (32), поэтому **O(32) ≈ O(1)**.
   - Побитовые операции выполняются за **O(1)**.

✅ Пространственная сложность: **O(1)**
   - Используем **фиксированное количество переменных**.

📌 **Как работает алгоритм?**
1️⃣ **Обнуляем `result`** и **проходим по каждому биту `n`**.
2️⃣ **Сдвигаем `result` влево** (`<< 1`), освобождая место для нового бита.
3️⃣ **Добавляем крайний бит `n` в `result`** (`| (n & 1)`, получая `0` или `1`).
4️⃣ **Сдвигаем `n` вправо** (`>> 1`), удаляя обработанный бит.
5️⃣ Повторяем **32 раза**, так как `n` — **32-битное число**.

💡 **Почему мы сдвигаем `result` влево?**
- Мы **поочередно добавляем** биты справа налево.
- Например, если `n = 110`, то:
  - `result = 0`
  - `result = (0 << 1) | 0 = 0`
  - `result = (0 << 1) | 1 = 1`
  - `result = (1 << 1) | 1 = 11`

"""
