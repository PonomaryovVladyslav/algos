# Задача: Найти число, которое встречается **только один раз**.
# Дано: массив `nums`, где все элементы **повторяются дважды**, кроме **одного**.
# Нужно: вернуть этот уникальный элемент.

def single_number(nums: list[int]) -> int:
    """
    Функция находит число, встречающееся один раз (используя XOR).

    🔹 Временная сложность: O(n), так как мы проходим по массиву один раз.
    🔹 Пространственная сложность: O(1), так как используем только одну переменную.

    :param nums: list[int] - массив, в котором все элементы встречаются дважды, кроме одного.
    :return: int - уникальное число.
    """

    result: int = 0
    for num in nums:
        result ^= num  # Применяем XOR ко всем элементам
    return result


# 🔹 Тестируем
print(single_number([2, 2, 1]))  # ✅ Ожидаемый результат: 1
print(single_number([4, 1, 2, 1, 2]))  # ✅ Ожидаемый результат: 4
print(single_number([1]))  # ✅ Ожидаемый результат: 1
print(single_number([0, 1, 0, 1, 99]))  # ✅ Ожидаемый результат: 99


"""
🔹 Анализ сложности:

✅ Временная сложность: **O(n)**
   - Мы проходим по массиву **ровно один раз**.
   - Побитовая операция XOR выполняется за **O(1)**.

✅ Пространственная сложность: **O(1)**
   - Используем **только одну переменную `result`**.

📌 **Как работает XOR?**
1️⃣ `A ^ A = 0` (XOR одинаковых чисел даёт `0`).  
2️⃣ `A ^ 0 = A` (XOR с `0` оставляет число неизменным).  
3️⃣ **XOR коммутативен и ассоциативен**, поэтому порядок неважен.

💡 **Почему этот метод работает?**
- Если элемент встречается **дважды**, то `num ^ num = 0`, поэтому он **исчезает**.
- Уникальный элемент **остаётся в `result`**, так как `0 ^ x = x`.

🔹 **Пример работы XOR**:
- `nums = [4, 1, 2, 1, 2]`
- `4 ^ 1 ^ 2 ^ 1 ^ 2`
- `= (4 ^ 4) ^ (1 ^ 1) ^ (2 ^ 2) = 0 ^ 0 ^ 0 ^ 4 = 4` ✅

"""
