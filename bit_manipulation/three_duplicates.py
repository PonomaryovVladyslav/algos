# Задача: Найти число, которое встречается **только один раз**.
# Дано: массив `nums`, где все элементы встречаются **ровно три раза**, кроме одного.
# Нужно: вернуть это уникальное число.

def find_single_number(nums: list[int]) -> int:
    """
    Функция находит число, встречающееся один раз (используя битовые операции).

    🔹 Временная сложность: O(n), так как мы проходим по массиву один раз.
    🔹 Пространственная сложность: O(1), так как используем только две переменные.

    :param nums: list[int] - массив, в котором все элементы встречаются трижды, кроме одного.
    :return: int - уникальное число.
    """

    ones: int = 0  # Число, которое появилось один раз
    twos: int = 0  # Число, которое появилось два раза

    for num in nums:
        ones = (ones ^ num) & ~twos  # Обновляем `ones`, сбрасываем если число встречается 3 раза
        twos = (twos ^ num) & ~ones  # Обновляем `twos`, сбрасываем если число встречается 3 раза

    return ones  # `ones` содержит единственное число


# 🔹 Тестируем
nums1 = [2, 2, 3, 2]
nums2 = [0, 1, 0, 1, 0, 1, 99]
nums3 = [7, 7, 7, 5]

print(find_single_number(nums1))  # ✅ Ожидаемый результат: 3
print(find_single_number(nums2))  # ✅ Ожидаемый результат: 99
print(find_single_number(nums3))  # ✅ Ожидаемый результат: 5


"""
🔹 Анализ сложности:

✅ Временная сложность: **O(n)**
   - Мы проходим по массиву **ровно один раз**.
   - Побитовые операции выполняются за **O(1)**.

✅ Пространственная сложность: **O(1)**
   - Используем **только две переменные** (`ones` и `twos`).

📌 **Как работает битовая логика?**
1️⃣ `ones ^= num`: Добавляет `num` в `ones`, если он встречается **впервые**.  
2️⃣ `twos ^= num`: Добавляет `num` в `twos`, если он **уже есть в `ones`**.  
3️⃣ `~twos`: Очищает `ones`, если `num` появился **трижды**.  
4️⃣ `~ones`: Очищает `twos`, если `num` появился **трижды**.  

🔹 **Пример работы (nums = [2, 2, 3, 2])**:
"""