# Задача: Сгенерировать все возможные комбинации `k` чисел от 1 до `n`.
# Дано: два числа `n` и `k`.
# Нужно: вернуть список всех возможных комбинаций длины `k`.

def combine(n: int, k: int) -> list[list[int]]:
    """
    Функция генерирует все возможные сочетания `k` элементов из `n`.

    :param n: int - число, от 1 до `n`, из которого выбираем элементы.
    :param k: int - длина каждой комбинации.
    :return: list[list[int]] - список всех возможных комбинаций длины `k`.
    """

    result: list[list[int]] = []  # Итоговый список комбинаций

    def backtrack(start: int, path: list[int]) -> None:
        """
        Рекурсивная функция для генерации комбинаций.

        :param start: int - число, с которого начинается перебор.
        :param path: list[int] - текущая комбинация.
        """

        if len(path) == k:  # Если выбрали k чисел, добавляем комбинацию в результат
            result.append(path[:])  # Копируем `path`, чтобы не изменять его
            return

        for i in range(start, n + 1):  # Перебираем числа от `start` до `n`
            path.append(i)  # Добавляем число в комбинацию
            backtrack(i + 1, path)  # Рекурсивно выбираем следующее число
            path.pop()  # Откатываем последний шаг (backtracking)

    backtrack(1, [])
    return result


# 🔹 Тестируем
print(combine(4, 2))  # Ожидаемый результат: [[1,2], [1,3], [1,4], [2,3], [2,4], [3,4]]
print(combine(3, 3))  # Ожидаемый результат: [[1,2,3]]
print(combine(5, 3))  # Ожидаемый результат: [[1,2,3], [1,2,4], [1,2,5], [1,3,4], ...]

"""
Анализ сложности:
- Временная сложность: O(C(n, k)) = O(n! / (k! * (n - k)!)), так как генерируем все сочетания.
- Пространственная сложность: O(k), так как максимальная глубина рекурсии — `k`.
"""
