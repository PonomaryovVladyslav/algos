# Задача: Сгенерировать все возможные комбинации букв для введенных цифр по аналогии с телефонной клавиатурой.
# Дано: строка `digits`, состоящая из цифр от 2 до 9.
# Нужно: вернуть все возможные комбинации букв, соответствующих введенным цифрам.

def letter_combinations(digits: str) -> list[str]:
    """
    Функция генерирует все возможные комбинации букв для введенных цифр.

    :param digits: str - строка цифр от 2 до 9.
    :return: list[str] - список возможных комбинаций букв.
    """

    if not digits:
        return []  # Если строка пустая, возвращаем пустой список

    phone_map: dict[str, str] = {
        "2": "abc", "3": "def", "4": "ghi", "5": "jkl",
        "6": "mno", "7": "pqrs", "8": "tuv", "9": "wxyz"
    }

    result: list[str] = []  # Итоговый список комбинаций

    def backtrack(index: int, path: list[str]) -> None:
        """
        Рекурсивная функция для генерации комбинаций.

        :param index: int - текущая позиция в строке `digits`.
        :param path: list[str] - текущая комбинация букв.
        """

        if index == len(digits):  # Дошли до конца, добавляем комбинацию в результат
            result.append("".join(path))
            return

        for letter in phone_map[digits[index]]:  # Перебираем все буквы, соответствующие текущей цифре
            backtrack(index + 1, path + [letter])  # Рекурсивный вызов с новой буквой

    backtrack(0, [])
    return result


# 🔹 Тестируем
print(letter_combinations("23"))  # Ожидаемый результат: ["ad", "ae", "af", "bd", "be", "bf", "cd", "ce", "cf"]
print(letter_combinations(""))    # Ожидаемый результат: []
print(letter_combinations("2"))   # Ожидаемый результат: ["a", "b", "c"]

"""
Анализ сложности:
- Временная сложность: O(4^n), где `n` — длина `digits`, так как каждая цифра может давать до 4 букв.
- Пространственная сложность: O(n), так как глубина рекурсии ограничена `n`.
"""
