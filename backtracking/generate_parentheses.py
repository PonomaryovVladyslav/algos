# Задача: Сгенерировать все возможные комбинации правильных скобочных последовательностей длины `2 * n`.
# Дано: число `n`, которое задает количество пар скобок.
# Нужно: вернуть список всех возможных корректных комбинаций скобок.

def generate_parenthesis(n: int) -> list[str]:
    """
    Функция генерирует все возможные комбинации правильных скобочных последовательностей длины `2 * n`.

    :param n: int - количество пар скобок.
    :return: list[str] - список возможных комбинаций скобок.
    """

    result: list[str] = []  # Итоговый список последовательностей

    def backtrack(path: list[str], open_count: int, close_count: int) -> None:
        """
        Рекурсивная функция для генерации последовательностей скобок.

        :param path: list[str] - текущая последовательность скобок.
        :param open_count: int - количество открывающих скобок в path.
        :param close_count: int - количество закрывающих скобок в path.
        """

        if len(path) == 2 * n:  # Если строка достигла нужной длины
            result.append("".join(path))  # Преобразуем список в строку и добавляем в результат
            return

        if open_count < n:  # Можно добавить '('
            path.append("(")
            backtrack(path, open_count + 1, close_count)
            path.pop()  # Откат (backtracking)

        if close_count < open_count:  # Можно добавить ')', если закрывающих меньше, чем открытых
            path.append(")")
            backtrack(path, open_count, close_count + 1)
            path.pop()  # Откат (backtracking)

    backtrack([], 0, 0)
    return result


# 🔹 Тестируем
print(generate_parenthesis(3))
# Ожидаемый результат: ["((()))", "(()())", "(())()", "()(())", "()()()"]

print(generate_parenthesis(1))
# Ожидаемый результат: ["()"]

"""
Анализ сложности:
- Временная сложность: O(4^n / sqrt(n)), так как мы генерируем все возможные правильные последовательности.
- Пространственная сложность: O(n), так как глубина рекурсии ограничена `n`.
"""
