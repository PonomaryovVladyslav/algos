def plus_one(digits: list[int]) -> list[int]:
    """
    Увеличивает число, представленное массивом цифр, на 1.

    🔹 **Алгоритм**:
       - Итерируем массив справа налево.
       - Если цифра `< 9`, просто увеличиваем и выходим.
       - Если цифра `9`, заменяем её на `0` и переносим `1` дальше.
       - Если все цифры были `9` (например, `999` → `1000`), добавляем `1` в начало.

    🔹 **Сложность**:
       - `O(n)`, так как в худшем случае мы проходим весь массив (например, `[9,9,9]`).
    """
    for i in range(len(digits) - 1, -1, -1):
        if digits[i] < 9:  # Если цифра < 9, просто увеличиваем и возвращаем
            digits[i] += 1
            return digits
        digits[i] = 0  # Иначе превращаем в 0 и переносим 1 дальше

    return [1] + digits  # Если все цифры были 9 (например, 999 → 1000)


# 🔹 Тесты
print(plus_one([1,2,3]))  # ✅ [1,2,4]
print(plus_one([9,9,9]))  # ✅ [1,0,0,0]
print(plus_one([4,3,2,1]))  # ✅ [4,3,2,2]
print(plus_one([0]))  # ✅ [1]
print(plus_one([8,9,9]))  # ✅ [9,0,0]
print(plus_one([9]))  # ✅ [1,0]
